#include <stdio.h>

    int main(){

        char *nome[3] = {"IO", "X", "KOBU"};

            for(int i = 0; i <= 3; i++){
                printf("%s \n", nome[i]);
            }
        return 0;
    }